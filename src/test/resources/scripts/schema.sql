CREATE TABLE Car (
                     carId VARCHAR(255) PRIMARY KEY,
                     brand VARCHAR(255),
                     model VARCHAR(255),
                     year INT,
                     price DOUBLE,
                     isAvailable BOOLEAN
);